set timeout=3
set default=0

menuentry "Luminary (initrd)" {
    multiboot /boot/kernel.bin
    module /boot/initrd.img initrd
    boot
}

menuentry "Luminary (disk)" {
    multiboot /boot/kernel.bin root=/dev/hda1
    boot
}
